#include<iostream>
using namespace std;
int sum(int a,int b)
{
  return a + b;
}
int main()
{
  cout<<"Hello World";

  int a ,b;
  cin>>a>>b;
  cout<<"Sum of the two number  = " << sum(a,b);
  return 0;
}
